class EntryPoint
{
    static void Main()
    {
        bool notAlive = false; // its Alive
        bool isAlive = true; // its Alive

        bool isEqual = (0 == 0);

        System.Console.WriteLine(notAlive);
        System.Console.WriteLine(isAlive);
        System.Console.WriteLine(isEqual);
    }
}